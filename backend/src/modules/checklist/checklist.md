# Checklist

Tabla independiente para plantillas descargables. No se relaciona con activos ni guarda resultados de revision.

Endpoints: CRUD en `/api/checklist` y descarga en `GET /api/checklist/:id/download`.
