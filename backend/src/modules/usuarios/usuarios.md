# Usuarios

Personas que existen en el sistema y pueden tener activos asignados. Se relacionan con cargo, dimension y cuenta.

Endpoints: `GET/POST /api/usuarios`, `GET/PUT/DELETE /api/usuarios/:id`.
