# Procesadores

Catalogo opcional para activos que representan computadores, notebooks o equipos con CPU identificable.

Endpoints: `GET/POST /api/procesadores`, `GET/PUT/DELETE /api/procesadores/:id`.
