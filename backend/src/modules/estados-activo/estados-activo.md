# Estados Activo

Catalogo de estados logicos del activo fijo: disponible, asignado, en reparacion, baja o perdido.

Endpoints: `GET/POST /api/estados-activo`, `GET/PUT/DELETE /api/estados-activo/:id`.
